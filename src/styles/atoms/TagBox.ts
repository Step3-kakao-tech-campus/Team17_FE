import styled from 'styled-components';

export const Tag = styled.button`
  border-radius: 5px;
  padding: 0.2rem 0.5rem;
  font-family: 'Gowun';
  font-size: 0.6rem;
  margin-right: 0.5rem;

  &:focus {
    outline: none;
  }
`;
