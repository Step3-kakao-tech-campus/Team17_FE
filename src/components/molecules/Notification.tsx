import React from 'react';

// api/profile/notifications
type NotificationProps = {
  className: string;
};

const NotificationGroup = ({ className }: NotificationProps) => {
  return <div>Notification</div>;
};

export default NotificationGroup;
